# 汤淡采集使用教程

适用版本：0.3.9。建议先完成「安装 → 飞书配置 → 单条作品同步」，确认自己的账号能正常写入，再按需开启批量采集、私聊监控或逐字稿。

## 1. 安装与准备

准备 Chrome 浏览器、已登录的抖音 / [热点宝](https://douhot.douyin.com/) 账号，以及自己可编辑的飞书多维表格。逐字稿功能另需阿里云百炼 DashScope API Key。

1. 在 GitHub 仓库点击 **Code → Download ZIP**，下载并解压。
2. 在 Chrome 地址栏打开 `chrome://extensions/`。
3. 开启「开发者模式」，点击「加载已解压的扩展程序」。
4. 选择里面直接包含 `manifest.json` 的文件夹。
5. 在浏览器扩展菜单固定「汤淡采集」，刷新已打开的抖音和热点宝页面。

不用安装 Node.js，不用执行构建命令。更新时替换原目录文件，在扩展管理页重新加载，再刷新采集页面。

## 2. 配置自己的飞书表格

插件区分两类表：

| 数据 | 写入位置 |
| --- | --- |
| 达人信息与粉丝画像 | 设置页 `table_id` 指定的数据表 |
| 主页作品、单条视频、私聊分享与逐字稿 | 同一 Base 的「竞品达人视频数据」表，缺少时自动创建 |

### 推荐：多维表格授权码

1. 打开目标多维表格，在「自定义插件 / 获取授权码」入口取得自己的 `pt-...` 授权码。
2. 点击插件图标 →「飞书与逐字稿设置」。
3. 确认「收集人名称」，默认是「汤淡」，每条新入库视频会记录该名字。
4. 授权方式选择「多维表格授权码（推荐）」，填写授权码。
5. 填写目标 Base 的 `app_token / base_token` 与达人信息表的 `table_id`。首次安装没有预设目标。
6. 也可以粘贴 `https://xxx.feishu.cn/base/bascnxxxx?table=tblxxxx`，点击「从 URL 填入」。
7. 点击「测试连接」，确认能读取字段数量，然后点击「保存设置」。

**测试连接不会保存配置，也没有验证真实写入。** 完成后按第 3 节同步一条作品，打开飞书确认结果。

如果拿到的是 `/wiki/...` 链接，路径中的 token 只是知识库节点标识，不能直接填作 `base_token`。请取得底层多维表格的真实 token，或使用该表的 `/base/...` 链接。切换目标表后重新检查 Base 和 `table_id` 是否对应，再测试、保存。

### 可选：开放平台应用

选择「飞书开放平台 App ID / Secret」，填写应用的 App ID、App Secret、目标 Base token 和 `table_id`。应用需要多维表格读写权限，以及目标表格的访问、编辑权限；按飞书应用配置将应用 / 机器人加入可访问目标表的协作者。然后测试并保存。

## 3. 先试一条作品

1. 打开一个自己可访问的抖音视频详情页，如 `https://www.douyin.com/video/...`。
2. 点击右下角面板或插件弹窗中的「同步当前作品」。首次验证可先不勾选热评和转写。
3. 等待结果提示，打开飞书同一 Base 的「竞品达人视频数据」表。
4. 确认标题、视频链接、互动数据和收集人已写入。

同一视频再次同步时按视频链接更新原记录。这里只保存结构化信息和链接，不会下载视频文件到电脑。

## 4. 采集达人与竞品作品

### 采集达人信息

登录热点宝，打开达人详情页，点击右下角「同步到飞书」；也可以打开抖音达人主页，点插件里的「提取并同步飞书」，跳转热点宝后自动采集。

「输入链接提取」可使用完整抖音主页链接、热点宝详情链接，也会尝试解析分享文本中的短链。短链需要能解析到公开达人 ID；失败时先在浏览器打开，复制公开主页完整链接。`/user/self` 不能替代公开主页链接，纯口令或任意分享文本不保证可识别。

### 批量采集主页作品

1. 打开目标达人抖音主页。
2. 点击「同步主页作品」；需要挑选内容时点击「筛选作品同步」。
3. 筛选面板支持发布日期区间、最低点赞数（如 `70000` 表示 7 万）、按点赞或评论从高到低排序。
4. 按需勾选「抓取前3条热评」和本次逐字稿选项，再开始同步。
5. 在飞书「竞品达人视频数据」表查看结果。

采集会尝试读取接口数据，必要时滚动页面兜底；「同步主页作品」不意味着能取得账号所有历史作品。登录态、页面可访问范围和平台限制都会影响结果。

热评会尝试直接读取接口，必要时复用后台视频标签页读取。取得点赞数时按点赞排序取前 3 条，否则按当前评论展示顺序取前 3 条；可能少于 3 条或没有结果。

## 5. 用私聊收集灵感

1. 打开「飞书与逐字稿设置」。
2. 填写「目标私聊名称」，必须和抖音会话列表完全一致；默认示例为「AI相关入库」。
3. 填写「自己的抖音昵称」，必须和发送分享的本人昵称一致；默认是「汤淡」。
4. 勾选「启用私聊自动监控」，保存设置，点击「打开监控页」。
5. 在专用页面保持抖音登录，确认消息面板能够进入目标会话，并检查插件显示的监控状态。
6. 等待历史基线建立后，用对应本人账号向目标会话新转发一条视频，再查看飞书入库结果。

监控识别配置昵称发出的分享视频。首次运行只把当前可见消息记为基线，随后采集符合条件的新分享，不会批量导入历史聊天。保留 Chrome 和专用监控页运行；普通抖音标签页不能直接替代带监控标记的专用页面。

需要时可勾选「Chrome 启动时自动打开专用监控页」。首次安装该选项和监控开关都默认关闭。停止采集可在弹窗点击「关闭监控」。

## 6. 按需生成逐字稿

先在设置页填写阿里云百炼 DashScope API Key 并保存。模型为 `paraformer-v2`，视频文件地址会提交给阿里云，并产生对应服务调用费用。

| 使用方式 | 操作 |
| --- | --- |
| 私聊入库自动转写 | 开启「新监控视频入库后自动转逐字稿」 |
| 手动同步时转写 | 在单条 / 主页作品同步时勾选本次转写选项 |
| 已入库作品补转写 | 在飞书对应行勾选「转逐字稿」 |

私聊自动转写开关只控制私聊流程；手动采集是否转写由本次任务的勾选决定。表内请求约每分钟检查一次，电脑休眠或 Chrome 关闭时不会持续按时处理。

在飞书查看「逐字稿状态」；完成后正文写入「直接转逐字稿」。缺少可访问的视频文件地址、Key 不可用或服务返回错误时，转写可能失败。音频自动识别不等于人工校对稿，使用前请复核专名、数字和断句。

## 7. 字段说明

| 分类 | 主要字段 |
| --- | --- |
| 达人基础 | 采集时间、热点宝链接、抖音主页、博主名称、抖音号、总粉丝量、总获赞量、总作品数、签名、标签、达人ID |
| 近期表现 | 近30天平均点赞量、平均分享量、平均评论量 |
| 粉丝画像 | 男女占比、男女 TGI、城市与省份 TOP10、年龄、城市等级、设备价格与品牌 |
| 作品信息 | 标题、标签、博主名称、收集人、封面地址、点赞、评论、热评、收藏、分享、时长、发布日期、视频链接 |
| 转写 | 视频源网址、作品网址、转逐字稿、直接转逐字稿、逐字稿状态 |

默认按「达人ID」更新达人记录，按「视频链接」更新作品记录。勾选「缺字段时自动创建」后，插件会补充缺少的字段；已有字段不会被强制转换类型。总粉丝、获赞和作品数写入整数，近 30 天平均值保留小数。接口没有提供的字段可能为空。

## 8. 常见问题

**没有采集面板 / 无法加载扩展**：确认选中的是包含 `manifest.json` 的解压目录，在受支持的抖音 / 热点宝页面刷新。更新代码后还要重新加载扩展。

**连接成功但没有入库**：确认已经点「保存设置」，检查写入权限和字段类型。达人与作品位于不同数据表，作品应到同一 Base 的「竞品达人视频数据」查看。

**监控一直等待 / 没有采集**：核对目标会话名、本人昵称、登录态和专用页。已有消息可能属于首次基线，请在显示开始监控后新转发一条视频。

**逐字稿一直等待或失败**：先查看「逐字稿状态」，确认配置了有效 Key、该任务已勾选转写，并且 Chrome 正在运行。视频源无法被转写服务访问时也会失败。

**抖音号、画像或热评为空**：平台未返回或当前账号无法读取相关数据，插件不能保证补全。先确认原页面在当前登录态可正常访问。

**数字显示 `.00`**：飞书数字字段的显示格式与写入值不同；需要时把该列小数位设为 `0`。

**标签字段报错**：确认「标签」列为多选类型。已有文本列需要在飞书中手动调整，调整前保留已有数据。

**升级后还是旧名字**：在设置页改为自己的昵称 / 收集人并保存。浏览器已保存配置不会随源文件替换自动重置。

授权码、App Secret、API Key 保存在本机扩展存储，使用时发送给相应服务。只填写自己的凭据，不把它们写进源码、截图或公开仓库。实际飞书写入与转写结果应以自己的账号运行验证为准。
