# 汤淡采集｜热点宝 + 私聊监控

这是「汤淡采集」的本地完整版本，包含热点宝采集、作品同步、私聊监控和逐字稿功能。

## 安装

1. 打开 `chrome://extensions/`，开启开发者模式。
2. 点击「加载已解压的扩展程序」。
3. 选择：包含 `manifest.json` 的「汤淡热点宝采集」文件夹。
4. Chrome 会提示扩展使用「调试程序」权限；这是自动点击抖音私聊和视频卡片所需权限。
5. 打开扩展设置页，重新填入自己的多维表格授权码并保存。不同扩展之间不会共享 Chrome 本地设置，所以必须重新填一次。

## 推荐设置

- 多维表格 `base_token`：填写你自己的多维表格 token。
- 数据表 `table_id`：填写接收达人信息的数据表 ID。
- 私聊自动监控：默认关闭，填好自己的飞书配置和会话名称后再启用。
- 收集人：默认「汤淡」，可修改；也可以直接在汤淡图标弹窗里保存。
- 目标私聊名称：`AI相关入库`。
- 自己的抖音昵称：`汤淡`。
- Chrome 启动时自动打开专用监控页：默认关闭，按需启用。

私聊监控的视频会自动写入同一个 Base 里的「竞品达人视频数据」表，并按「视频链接」查重。它不使用上面的达人主页 `table_id` 作为视频表。

## 验证与切换

1. 点击设置页的「测试连接」，成功后点击「保存设置」，再启用监控。
2. 点击「打开监控页」，等待监控状态显示正在监控。
3. 转发一个以前没有转发过的视频到目标私聊。
4. 在「竞品达人视频数据」中确认标题、博主、互动数、发布日期和视频链接均已写入。
5. 验证成功后，再停用以下两个旧组件，避免重复采集：
   - 之前安装的旧版采集扩展。
   - 独立的「抖音转发 → 飞书达人库」扩展。

独立本机服务在整合版中不再需要；确认整合版稳定后再停用即可。

## 运行条件

- Chrome 保持登录抖音。
- 电脑保持唤醒，Chrome 不退出。
- 专用监控页不要手动关闭；关闭后扩展会在下一次周期检查时重新打开。
- 整合版使用独立地址参数 `dycollector=tangdan`，不会与旧版独立监控页共用同一个标签页。
- 0.2.3 起，飞书暂时写入失败时会保留在后台重试队列，不再反复点击同一个视频导致播放/暂停抖动。
- 0.2.4 起，点击汤淡图标即可用“开启监控 / 关闭监控”按钮立即控制私聊监控。
- 0.2.5 起，同一条消息只会排队一次；视频 ID 一解析到就会立即暂停并关闭，飞书写入改在关闭后继续，连续转发不会再卡在上一条播放/暂停。
- 0.3.0 起，可在插件弹窗勾选“入库后自动转逐字稿”，并在设置页保存阿里云百炼 DashScope API Key。插件使用 paraformer-v2 后台转写，并把文字与处理状态回写到同一条飞书视频记录。
- 0.3.1 起，完整文字改为写入用户已有的“直接转逐字稿”文本列；处理进度继续写入“逐字稿状态”。
- 0.3.2 起，监控维护循环增加互斥锁，避免高频扫描期间反复开关抖音消息面板。
- 0.3.3 起，逐字稿优先使用作品 `play_addr.uri` 生成无水印 `/aweme/v1/play/` 源地址，避免临时 CDN 或 `/playwm/` 地址被阿里云服务器拒绝；能识别旧 `/playwm/` 地址的 403 任务会在插件重载后自动修复并重试一次。
- 0.3.4 会在升级后一次性重新解析当前目标群的最新转发，用新视频源替换无法恢复 URI 的旧 403 任务；成功入队后即记录完成，不会在日常刷新时重复打开。
- 0.3.5 起，可在插件弹窗或设置页填写当前“收集人”。每台电脑独立保存，视频入库和后续逐字稿回填都会保留该视频最初的收集人。
- 0.3.6 起，单条视频优先合并当前详情接口、页面 JSON 和可见信息，不再拿到详情后仍扫描作者全部作品；只有当前页完全无法读取时才启用作者列表兜底，并在找到目标视频后立即停止翻页。
- 0.3.7 起，插件弹窗和网页浮动面板增加“本次作品同步后转逐字稿”。单条视频与主页批量作品均可按次勾选；主页批量勾选会为本次筛选出的每条作品创建转写任务并产生相应费用，默认不勾选。
- 0.3.8 起，飞书「竞品达人视频数据」增加“转逐字稿”复选框。对以前未转写的视频勾选后，配置了阿里云 Key 的插件会在约 1 分钟内补取视频源并创建任务；任务接收后自动取消勾选，进度和结果继续回填原行。
- 同一视频重复转发会更新同一条飞书记录，不会重复新增。
